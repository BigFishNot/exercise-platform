<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.exercise.mappers.MailConfigMapper">

    <resultMap id="mailConfigMap" type="com.exercise.campus.entity.po.MailConfig">
        <id property="id" column="id"/>
        <result property="smtpHost" column="smtp_host"/>
        <result property="smtpPort" column="smtp_port"/>
        <result property="smtpUsername" column="smtp_username"/>
        <result property="smtpPassword" column="smtp_password"/>
        <result property="sender" column="sender"/>
        <result property="useSsl" column="use_ssl"/>
        <result property="sendTimePoints" column="send_time_points"/>
        <result property="enabled" column="enabled"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <insert id="upsert" parameterType="com.exercise.campus.entity.po.MailConfig">
        INSERT INTO mail_config (
            id, smtp_host, smtp_port, smtp_username, smtp_password,
            sender, use_ssl, send_time_points, enabled, create_time, update_time
        ) VALUES (
            1, #{smtpHost}, #{smtpPort}, #{smtpUsername}, #{smtpPassword},
            #{sender}, #{useSsl}, #{sendTimePoints}, #{enabled}, NOW(), NOW()
        )
        ON DUPLICATE KEY UPDATE
            smtp_host = VALUES(smtp_host),
            smtp_port = VALUES(smtp_port),
            smtp_username = VALUES(smtp_username),
            smtp_password = VALUES(smtp_password),
            sender = VALUES(sender),
            use_ssl = VALUES(use_ssl),
            send_time_points = VALUES(send_time_points),
            enabled = VALUES(enabled),
            update_time = NOW()
    </insert>

    <select id="select" resultMap="mailConfigMap">
        SELECT * FROM mail_config WHERE id = 1 LIMIT 1
    </select>
</mapper>