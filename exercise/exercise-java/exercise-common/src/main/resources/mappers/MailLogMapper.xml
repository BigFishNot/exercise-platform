<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.exercise.mappers.MailLogMapper">

    <resultMap id="mailLogMap" type="com.exercise.campus.entity.po.MailLog">
        <id property="logId" column="log_id"/>
        <result property="userId" column="user_id"/>
        <result property="userEmail" column="user_email"/>
        <result property="templateId" column="template_id"/>
        <result property="templateType" column="template_type"/>
        <result property="sendDate" column="send_date"/>
        <result property="triggerType" column="trigger_type"/>
        <result property="operatorId" column="operator_id"/>
        <result property="status" column="status"/>
        <result property="errorSummary" column="error_summary"/>
        <result property="renderedTitle" column="rendered_title"/>
        <result property="createTime" column="create_time"/>
    </resultMap>

    <sql id="allColumns">
        log_id, user_id, user_email, template_id, template_type,
        send_date, trigger_type, operator_id, status, error_summary,
        rendered_title, create_time
    </sql>

    <insert id="insert" parameterType="com.exercise.campus.entity.po.MailLog">
        INSERT INTO mail_log (
            user_id, user_email, template_id, template_type,
            send_date, trigger_type, operator_id, status, error_summary,
            rendered_title, create_time
        ) VALUES (
            #{userId}, #{userEmail}, #{templateId}, #{templateType},
            #{sendDate}, #{triggerType}, #{operatorId}, #{status}, #{errorSummary},
            #{renderedTitle}, NOW()
        )
    </insert>

    <select id="countByQuery" resultType="int">
        SELECT COUNT(1) FROM mail_log l
        <where>
            <if test="status != null">AND l.status = #{status}</if>
            <if test="templateType != null">AND l.template_type = #{templateType}</if>
            <if test="triggerType != null">AND l.trigger_type = #{triggerType}</if>
            <if test="userAccount != null and userAccount != ''">
                AND EXISTS (SELECT 1 FROM user_info u WHERE u.user_id = l.user_id AND u.account LIKE CONCAT('%', #{userAccount}, '%'))
            </if>
        </where>
    </select>

    <select id="selectList" resultMap="mailLogMap">
        SELECT <include refid="allColumns"/> FROM mail_log l
        <where>
            <if test="status != null">AND l.status = #{status}</if>
            <if test="templateType != null">AND l.template_type = #{templateType}</if>
            <if test="triggerType != null">AND l.trigger_type = #{triggerType}</if>
            <if test="userAccount != null and userAccount != ''">
                AND EXISTS (SELECT 1 FROM user_info u WHERE u.user_id = l.user_id AND u.account LIKE CONCAT('%', #{userAccount}, '%'))
            </if>
        </where>
        ORDER BY l.create_time DESC
    </select>

    <select id="countTodaySuccess" resultType="int">
        SELECT COUNT(1) FROM mail_log
        WHERE user_id = #{userId} AND template_type = #{templateType}
          AND send_date = #{sendDate} AND status = 1
    </select>

    <select id="countByUserTypeDate" resultType="int">
        SELECT COUNT(1) FROM mail_log
        WHERE user_id = #{userId} AND template_type = #{templateType}
          AND send_date = #{sendDate} AND status = #{status}
    </select>
</mapper>